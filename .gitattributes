# Normalise to LF in the repository. The CI workflow runs shell scripts on
# Linux, so a CRLF checkout of anything it executes would fail there and pass
# on the author's machine.
* text=auto eol=lf

*.ps1 text eol=crlf

*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
